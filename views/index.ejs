<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>spider maneiro</title>
</head>
<body>
    <% if (dados.title.length < 1) { %>
        <ul>
        <% for(var i = 0; dados.title.length > i; i++ ) { %>
        
            <li> <%= dados.title %> </li>

        <% } %>
        </ul>

    <% } %>

</body>
</html>